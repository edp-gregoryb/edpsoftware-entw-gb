import { TestBed } from '@angular/core/testing';

import { RestVlupdtobjtxtService } from './rest-vlupdtobjtxt.service';

describe('RestVlupdtobjtxtService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: RestVlupdtobjtxtService = TestBed.get(RestVlupdtobjtxtService);
    expect(service).toBeTruthy();
  });
});
