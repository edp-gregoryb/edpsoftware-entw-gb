import { TestBed } from '@angular/core/testing';

import { RestVlgetobjtxtService } from './rest-vlgetobjtxt.service';

describe('RestVlgetobjtxtService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: RestVlgetobjtxtService = TestBed.get(RestVlgetobjtxtService);
    expect(service).toBeTruthy();
  });
});
