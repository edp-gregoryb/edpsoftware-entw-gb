/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { SavetermindetailService } from './savetermindetail.service';

describe('SavetermindetailService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SavetermindetailService]
    });
  });

  it('should ...', inject([SavetermindetailService], (service: SavetermindetailService) => {
    expect(service).toBeTruthy();
  }));
});
