import { TestBed } from '@angular/core/testing';

import { RestinsdeleteofferteService } from './restinsdeleteofferte.service';

describe('RestinsdeleteofferteService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: RestinsdeleteofferteService = TestBed.get(RestinsdeleteofferteService);
    expect(service).toBeTruthy();
  });
});
