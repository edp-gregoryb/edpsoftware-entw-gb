import { TestBed } from '@angular/core/testing';

import { RestupdateadrhauptService } from './restupdateadrhaupt.service';

describe('RestupdateadrhauptService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: RestupdateadrhauptService = TestBed.get(RestupdateadrhauptService);
    expect(service).toBeTruthy();
  });
});
