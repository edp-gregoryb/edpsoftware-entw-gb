import { TestBed } from '@angular/core/testing';

import { RestcreateadrhauptService } from './restcreateadrhaupt.service';

describe('RestcreateadrhauptService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: RestcreateadrhauptService = TestBed.get(RestcreateadrhauptService);
    expect(service).toBeTruthy();
  });
});
