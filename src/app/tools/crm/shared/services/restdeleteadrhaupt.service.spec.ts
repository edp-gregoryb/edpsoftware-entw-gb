import { TestBed } from '@angular/core/testing';

import { RestdeleteadrhauptService } from './restdeleteadrhaupt.service';

describe('RestdeleteadrhauptService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: RestdeleteadrhauptService = TestBed.get(RestdeleteadrhauptService);
    expect(service).toBeTruthy();
  });
});
